<div class="home">
    <h1>The Tech Blog</h1>
    {{#each posts}}
    <div class="post">
        <h2>{{this.title}}</h2>
        <p>{{this.content}}</p>
        <small>Posted by {{this.user.username}} on {{this.createdAt}}</small>
    </div>
    {{/each}}
</div>
